'use client'
import { useEffect, useRef } from 'react'
type P={x:number;y:number;vx:number;vy:number;r:number;d:number}
export function ParticleField(){const ref=useRef<HTMLCanvasElement>(null);useEffect(()=>{const el=ref.current;if(!el)return;const c=el.getContext('2d');if(!c)return;let ps:P[]=[],raf=0,mx=-9999,my=-9999;const resize=()=>{const r=el.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,1.5);el.width=r.width*d;el.height=r.height*d;c.setTransform(d,0,0,d,0,0);ps=Array.from({length:Math.min(150,Math.floor(r.width/5))},()=>({x:Math.random()*r.width,y:Math.random()*r.height,vx:(Math.random()-.5)*.22,vy:(Math.random()-.5)*.22,r:1+Math.random()*2.2,d:.35+Math.random()*.9}))};const move=(e:PointerEvent)=>{const r=el.getBoundingClientRect();mx=e.clientX-r.left;my=e.clientY-r.top};const draw=()=>{const r=el.getBoundingClientRect();c.fillStyle='#0d1110';c.fillRect(0,0,r.width,r.height);for(const p of ps){const dx=p.x-mx,dy=p.y-my,d2=dx*dx+dy*dy;if(d2<16000){const f=(1-d2/16000)*.6,m=Math.max(1,Math.sqrt(d2));p.vx+=dx/m*f;p.vy+=dy/m*f}p.vx*=.985;p.vy*=.985;p.x+=p.vx;p.y+=p.vy;if(p.x<0)p.x=r.width;if(p.x>r.width)p.x=0;if(p.y<0)p.y=r.height;if(p.y>r.height)p.y=0;c.beginPath();c.fillStyle=`rgba(216,191,139,${.28+p.d*.48})`;c.arc(p.x,p.y,p.r*p.d,0,Math.PI*2);c.fill()}raf=requestAnimationFrame(draw)};resize();draw();addEventListener('resize',resize);el.addEventListener('pointermove',move);return()=>{cancelAnimationFrame(raf);removeEventListener('resize',resize);el.removeEventListener('pointermove',move)}},[]);return <section className="particles"><canvas ref={ref}/><div className="particlecopy"><span>POINTER FIELD</span><h2>Depth without 3D overhead.</h2><p>Mueve el cursor sobre la escena.</p></div></section>}
